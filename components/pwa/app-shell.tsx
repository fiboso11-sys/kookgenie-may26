/**
 * Mobile-first shell pieces used by the main AppShell (see components/AppShell.tsx).
 * Keeps PWA navigation and scroll behavior in one import surface.
 */
export { MobileTabBar } from "@/components/pwa/mobile-tab-bar";
export { PullToRefresh } from "@/components/pwa/pull-to-refresh";
