/**
 * Emergency recovery: local Supabase `food_database` only — no Gemini, no USDA, no Open Food Facts.
 * Set to `false` when the AI / external stack is stable again.
 */
export const KG_EMERGENCY_DISABLE_AI = true;
